# OpenAI Runtime Secret

- Date: 2026-04-19
- Runtime AI credentials are sourced from `.env.local` via `src/lib/env.ts`.
- Keep `OPENAI_API_KEY` out of tracked source files and examples; rotate it in local env files only.
- The local `OPENAI_API_KEY` entry was updated on this date for the current workspace.
